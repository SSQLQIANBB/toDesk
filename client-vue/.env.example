# 前端本地开发环境变量模板
#
# 配置位置：client-vue/.env.local
# 创建方式：进入 client-vue 后执行 `cp .env.example .env.local`。
# 加载方式：Vite 启动时自动读取 .env.local；真实文件由 client-vue/.gitignore 中的 *.local 忽略。
# 安全要求：所有 VITE_* 值都会暴露在浏览器代码中，绝对不能填写密码、Token、AccessKey 或 SecretKey。
# 业务代码只允许从 src/config/env.ts 导入 publicEnv，不要直接读取 import.meta.env。

# [可选：HTTP API 基础地址]
# 前后端都在本机开发时留空，Vite 会把 /api 和 /uploads 代理到 http://localhost:3000。
# 后端位于其他机器时，填写浏览器可以访问的完整源地址，不要以 / 结尾。
VITE_API_BASE_URL=

# [可选：Socket.IO 地址]
# 前后端都在本机开发时留空；远程联调时填写与后端 Socket.IO 对应的完整源地址。
VITE_SOCKET_URL=
