# 桌面版嵌入本地前端，生产构建不能使用空地址（没有 Nginx 同源代理）。
# 必填；由部署负责人提供；公开地址可分享；不包含 /api 或 /meeting 后缀。
# 示例为本地联调，分发安装包前替换成已部署服务的 HTTPS 地址。
VITE_API_BASE_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
# 不要把数据库、JWT、七牛等服务端密钥放入任何 VITE_* 变量。
