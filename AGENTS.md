# 项目协作约定

- 当前处于内测阶段，自有应用和安装包使用带 `-beta.N` 的语义化版本，用户界面显示“内测版”。不要修改第三方依赖版本来添加内测标识。
- 提交代码使用中文提交说明。新增较大功能、重要协议或架构变更时，在该批提交中升级产品版本并同步所有版本来源；普通内测迭代递增 `beta.N`。版本维护命令和具体规则见 `docs/DESKTOP_RELEASE.md`。
- 远控入口仅按真实实现及验收结果开放。打包成功不能代替系统权限、授权、停止释放、实际传输和平台兼容性验收。
